if(${MPE_EDITOR_LIBRARY_TYPE} STREQUAL STATIC)
    add_library(MPE_EDITOR STATIC
        ${MPE_EDITOR_CPP}
        ${MPE_EDITOR_OSX_CPP}
    )

    target_compile_definitions(MPE_EDITOR PRIVATE MPE_EDITOR_STATIC_LIBRARY)
elseif(${MPE_EDITOR_LIBRARY_TYPE} STREQUAL SHARED)
    add_library(MPE_EDITOR SHARED
        ${MPE_EDITOR_CPP}
        ${MPE_EDITOR_OSX_CPP}
    )

    target_compile_definitions(MPE_EDITOR PUBLIC MPE_EDITOR_DYNAMIC_LIBRARY)
    target_compile_definitions(MPE_EDITOR PRIVATE MPE_EDITOR_BUILD_DLL)
else()
    message(FATAL_ERROR "LIBRARY TYPE {${MPE_EDITOR_LIBRARY_TYPE}} NOT SUPPORTED.")
endif()

target_compile_definitions(MPE_EDITOR PUBLIC MPE_PLATFORM_OSX)
target_compile_definitions(MPE_EDITOR PUBLIC MPE_ENABLE_ASSERTS)
target_compile_definitions(MPE_EDITOR PUBLIC MPE_COMPILER_${CURRENT_COMPILER})

# ENABLE PROFILING FOR DEBUG BUILS
if(CMAKE_BUILD_TYPE STREQUAL Debug)
    target_compile_definitions(MPE_EDITOR PUBLIC MPE_CORE_DEBUG_PROFILING)
    target_compile_definitions(MPE_EDITOR PUBLIC MPE_DEBUG_PROFILING)
    target_compile_definitions(MPE_EDITOR PUBLIC MPE_ENABLE_DEBUG_LOG)
endif(CMAKE_BUILD_TYPE STREQUAL Debug)

# GRAPHICS APIS
if(MPE_OPENGL)
    target_compile_definitions(MPE_EDITOR PUBLIC MPE_OPENGL)
endif()

if(MPE_OPENGLES)
    target_compile_definitions(MPE_EDITOR PUBLIC MPE_OPENGLES)
endif()

target_compile_definitions(MPE_EDITOR PUBLIC MPE_FULL_VERSION_${PROJECT_FULL_VERSION})

# CMAKE IS NOT INHERETING COMPILER FLAGS PROPERLY
set(MPE_EDITOR_PUBLIC_DEFINITIONS)

if(MPE_LIBRARY_TYPE STREQUAL SHARED)
    list(APPEND MPE_EDITOR_PUBLIC_DEFINITIONS MPE_EDITOR_DYNAMIC_LIBRARY)
else()
    list(APPEND MPE_EDITOR_PUBLIC_DEFINITIONS MPE_EDITOR_STATIC_LIBRARY)
endif()

list(APPEND MPE_EDITOR_PUBLIC_DEFINITIONS MPE_PLATFORM_OSX)
list(APPEND MPE_EDITOR_PUBLIC_DEFINITIONS MPE_ENABLE_ASSERTS)
list(APPEND MPE_EDITOR_PUBLIC_DEFINITIONS MPE_COMPILER_${CURRENT_COMPILER})

if(CMAKE_BUILD_TYPE STREQUAL Debug)
    list(APPEND MPE_EDITOR_PUBLIC_DEFINITIONS MPE_CORE_DEBUG_PROFILING MPE_DEBUG_PROFILING MPE_ENABLE_DEBUG_LOG)
endif()

# GRAPHICS APIS
if(MPE_OPENGL)
    list(APPEND MPE_EDITOR_PUBLIC_DEFINITIONS MPE_OPENGL)
endif()

if(MPE_OPENGLES)
    list(APPEND MPE_EDITOR_PUBLIC_DEFINITIONS MPE_OPENGLES)
endif()

list(APPEND MPE_EDITOR_PUBLIC_DEFINITIONS MPE_FULL_VERSION_${PROJECT_FULL_VERSION})

if(${VERBOSE})
    get_target_property(DEFINITIONS_LIST MPE_EDITOR COMPILE_DEFINITIONS)
    message("
-----------------------------------------------------------
MPE EDITOR COMPILE DEFINITIONS\n")

    foreach(DEFINITION ${DEFINITIONS_LIST})
        message(STATUS "${DEFINITION}")
    endforeach()

    message("
-----------------------------------------------------------")
endif()

target_compile_features(MPE_EDITOR PUBLIC ${CXX_VERSION_NAME})
set_target_properties(MPE_EDITOR PROPERTIES VERSION ${PROJECT_FULL_VERSION})

set_target_properties(MPE_EDITOR PROPERTIES OUTPUT_NAME "Editor")